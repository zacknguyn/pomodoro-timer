{
  "name": "pomogit-backend",
  "version": "1.0.0",
  "description": "Backend for the application Pomogit, a commit-based organizing for github.",
  "keywords": [
    "pomodoro"
  ],
  "license": "ISC",
  "author": "Zack",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "check": "node --check index.js && node --check src/app.js && node --check src/controllers/*.js && node --check src/lambdas/*.js && node --check src/lib/*.js && node --check src/middleware/*.js && node --check src/repositories/*.js && node --check src/services/*.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "@octokit/rest": "^22.0.1",
    "@vendia/serverless-express": "^4.12.6",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.19.2",
    "express-rate-limit": "^7.5.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.13.3",
    "zod": "^3.23.8"
  }
}
