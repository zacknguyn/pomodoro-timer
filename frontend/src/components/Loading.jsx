import React from "react";
const Loading = () => <div className="flex items-center justify-center h-screen mc-body text-xs uppercase tracking-widest text-text-muted">Loading Registry...</div>;
export default Loading;
